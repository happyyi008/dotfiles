
. ~/.bash_aliases
source  ~/git-completion.bash
set -o vi

getBranch() {
    if git rev-parse --git-dir > /dev/null 2>&1; then
        gb | grep '^*' | cut -c 3-
    fi
}

export PS1="[\w] \$(getBranch)\n\u ~>"

